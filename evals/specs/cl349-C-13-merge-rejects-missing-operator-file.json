{
  "name": "C-13 merge exits non-zero when --operator path does not exist on disk",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/merge-registry.sh\" --operator missing.yaml --emit merged.yaml 2>err.txt; rc=$?; [ \"$rc\" -ne 0 ] && echo rejected 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["rejected"]}
}
