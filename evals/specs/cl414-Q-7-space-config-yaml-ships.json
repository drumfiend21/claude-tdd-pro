{
  "name": "Q-7 cross-loop integration: space/config.yaml ships so cross-loop honors opt-in gates",
  "command": "[ -s \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" ] && echo ok 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["ok"]}
}
