{
  "name": "sync includes the _operator tree and _community plugins folders in the synced contents",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/skills/sync/list-contents.sh\" 2>out.txt && grep -q '_operator' out.txt && grep -q '_community' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
