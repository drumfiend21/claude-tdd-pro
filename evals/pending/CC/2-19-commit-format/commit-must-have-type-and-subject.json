{
  "name": "commit format: subject line must match `^(feat|fix|test|docs|refactor|chore|perf|ci|build)(\\([^)]+\\))?: .{1,72}$`",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/validate-commit-msg.sh\" \"$PWD/msg.txt\"",
  "setup": [
    "echo 'banana commit message' > msg.txt"
  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "type",
      "subject"
    ]
  }
}
