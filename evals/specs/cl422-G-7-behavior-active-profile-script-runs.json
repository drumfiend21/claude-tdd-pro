{
  "name": "G-7 profile system: profiles/active.sh exits with recognized status when invoked",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/profiles/active.sh\" 2>err.txt; rc=$?; [ \"$rc\" -le 2 ] && echo runs 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "runs"
    ]
  }
}
