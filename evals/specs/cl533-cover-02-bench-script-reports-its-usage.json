{
  "name": "scripts/bench.sh reports its usage on --help (entry exercised)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/scripts/bench.sh\" --help 2>&1 | grep -qi 'usage' && echo bench-usage 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "bench-usage"
    ]
  }
}
