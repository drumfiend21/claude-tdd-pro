{
  "name": "aggregator skips non-YAML files (e.g. .md, .txt, .json)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/aggregator.sh\" --root \"$PWD/tree\" --format json | node -e 'let s=\"\";process.stdin.on(\"data\",d=>s+=d);process.stdin.on(\"end\",()=>{const r=JSON.parse(s);const ids=r.rules.map(x=>x.id);if(ids.length===1&&ids[0]===\"g-rule\"){process.stderr.write(\"ok\\n\")}else{process.stderr.write(\"FAIL: ids=\"+JSON.stringify(ids)+\"\\n\");process.exit(1)}});'",
  "setup": [
    "mkdir -p tree/google",
    "cat > tree/google/g.yaml << 'EOF'\nsource:\n  id: g\n  authoritative_publisher: Google\n  authoritative_url: https://example.com/g\n  registry_link: STANDARDS-URLS.yaml\n  fetched_at: 2026-05-09T08:00:00Z\n  content_hash: sha256:g\nrules:\n  - id: g-rule\n    name: g\n    description: g rule.\n    detector: rubric/detectors/g.sh\nrecommended_set: [g-rule]\nall_set: [g-rule]\nEOF",
    "echo 'this is markdown not yaml' > tree/google/README.md",
    "echo 'plain text' > tree/google/notes.txt",
    "echo '{\"rules\":[{\"id\":\"json-rule\"}]}' > tree/google/data.json"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": ["ok"]
  }
}
