{
  "name": "Q-7 cross-loop integration: emit script references the collector or loop substrate",
  "command": "grep -qiE \"collector|loop|emit|event\" \"$CLAUDE_PLUGIN_ROOT/space/cross-loop-emit.sh\" && echo cross 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["cross"]}
}
