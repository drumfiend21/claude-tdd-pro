{
  "name": "Q-1 SPACE config: collaboration defaults to posture: opt-in per §10 Q-1 privacy posture",
  "command": "grep -A1 collaboration: \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" | grep -q \"posture: opt-in\" && echo opt_in 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["opt_in"]}
}
