{
  "name": "messageId: numeric data values are coerced to string for substitution",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule msgid-numeric --in \"$PWD/file.js\" --report-format json 2>out.json && grep -E \"line 42\" out.json 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'x' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
