{
  "name": "/postmortem: generates eval spec under evals/specs/<category>-<name>.json",
  "command": "echo \"g-node-002\" | bash \"$CLAUDE_PLUGIN_ROOT/commands/postmortem.sh\" \"naked throw\" --interactive && ls evals/specs/postmortem-*.json | head -1 | grep -q \".json\" && echo ok >&2",
  "setup": [
    "mkdir -p evals/specs"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
