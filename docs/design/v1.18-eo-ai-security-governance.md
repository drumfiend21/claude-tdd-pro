# v1.18 — AI security & governance (EO-aligned) design

**Status:** PROPOSED (2026-06-13). Full detail for the additive reference block at
`docs/architecture-v1.9.md` **§28**. This file carries the rationale, the EO→mechanism
mapping, the feature decomposition, the cross-cutting contracts, the spec sketches, and the
ticket plan. The §28 block in the constitution carries only what `CLAUDE.md` requires to be
extractable from the constitution itself (IDs, standard-form bullets, §25 vocab, anti-drift
folder map, §20 sequencing).

Per the append-only discipline (`docs/memory/feedback-additive-amendment-by-reference.md`):
this is new surface written as a NEW file plus an APPENDED §28 reference block. No existing
§1–§27 content is altered. Build (substrate + specs) does not begin until the operator ratifies.

---

## §28.0 Source of the amendment — the executive order

On **2026-06-02** the White House issued the executive order **"Promoting Advanced Artificial
Intelligence Innovation and Security"** (published in the Federal Register 2026-06-05). Its
stated policy is U.S. AI leadership through an **innovation-first, voluntary** posture —
**Sec. 1 expressly disclaims any mandatory licensing, preclearance, or permitting requirement**
for developing, releasing, or distributing AI models — paired with stronger cyber defenses. The
operative sections, as corroborated across the primary text and multiple independent legal
analyses (Covington/Inside Privacy, A&O Shearman, Hogan Lovells, DLA Piper, Wiley, Skadden,
Sidley, Crowell, WilmerHale, Tenable):

| EO section | Title | Core directive |
|---|---|---|
| **Sec. 2** | Upgrading American Systems for Advanced AI | CNSS prioritizes cyber defense of National Security Systems (30 days); **Treasury + NSA + CISA stand up an "AI Cybersecurity Clearinghouse" (30 days) focused on finding and fixing software vulnerabilities**. |
| **Sec. 3** | Secure Frontier Model Deployment | Classified **benchmarking** of advanced cyber capabilities (60 days); a threshold defining a **"covered frontier model"**; a **voluntary framework** giving the government **up to 30 days of pre-release access**, subject to **confidentiality, cybersecurity, insider-risk, and IP protections**, plus trusted-partner early access. |
| **Sec. 4** | Protection Against Criminal Actors | Attorney General prioritizes criminal enforcement against **AI-enabled cyber activity** — illegal computer access, identity fraud, wire fraud. |

Deadlines anchor the urgency: 30-day deliverables by **2026-07-02**, 60-day by **2026-08-01**.

**Why this is in scope for Claude TDD Pro.** The plugin is a test-first software-development
harness whose existing thesis is *grounded, cited, auditable, enforced-in-three-surfaces*
engineering. The EO's cyber-defense, vulnerability-remediation, provenance/IP-protection, and
voluntary-governance themes map directly onto surfaces the plugin already owns (the §2.8 AI
Provenance Manifest, the §2.9 control mapping, the O-6 signed audit checkpoints, the §2.24
portable audit-pack, the S-2 fetchers, and the §27.28/§27.29 three-surface enforcement pattern).
This amendment **composes** those primitives into EO-aligned features; it invents no new engine.

**Explicit scope boundary (anti-overreach).** The plugin does **not** run frontier models,
operate the federal clearinghouse, or make "covered frontier model" determinations — those are
government functions. Every feature below is a **developer-side governance/engineering surface**:
it helps an org *engage* the voluntary framework and *harden* its own software, grounded in
public authorities (NIST/CISA/SLSA/OpenSSF) with the plugin's standing **cite-or-decline**
discipline. Sec. 4 (DOJ enforcement) is not a software feature; its repo-relevant residue
(misuse-resistant audit trails) is folded into C-22's control table, not given a standalone ID.

---

## §28.1 Feature decomposition (EO→mechanism)

All IDs are the next free number in their phase (verified against the architecture: H≤13, S≤53,
C≤21, X≤9; contracts ≤§2.29). Each feature reuses existing substrate where the "reuses" column
names it.

### Phase H — cyber-defense hardening (EO Sec. 2 + Sec. 3 IP protections)

- **H-14 — Dependency vulnerability scan + remediation gate.** `commands/vuln-scan.sh` reads the
  project's declared dependencies (`package.json`/`package-lock.json`, `requirements.txt`,
  `go.mod`, `Cargo.toml`), matches them against an **injected, deterministic advisory dataset**
  (fixtures, exactly as S-21 injects HTTP responses and the platform boundaries inject native API
  responses — determinism is preserved; no live network in the test substrate), classifies each
  finding by CVSS severity, and **gates** per §2.30: `critical`/`high` block (exit 1), `medium`/
  `low` warn, none ⇒ green no-op (exit 0). Emits a remediation report naming the fixed-in version
  for each finding (the EO Sec. 2 "find **and fix**" loop). Grounded in CISA SSDF / NIST 800-53
  (S-54). *Reuses:* §2.2 detector exit-code contract, §2.4 `security` eval category.
- **H-15 — SBOM generation + signed provenance attestation.** `commands/sbom.sh` generates a
  **CycloneDX** (default) or **SPDX** software bill of materials for the build artifact and
  produces a **SLSA-style signed provenance attestation** linking the SBOM digest to the §2.8 AI
  Provenance Manifest. Supports the EO's "protect American ingenuity and IP" theme and Sec. 3's
  IP-protection condition on pre-release access. *Reuses:* O-6 audit-log signing key handling
  (H-4 threat model), the `slsa` namespace already in the §1 inventory, §2.8 manifest.
- **H-16 — Frontier-model pre-release governance readiness checklist.** `commands/frontier-eval.sh`
  emits an **operator-readable readiness scaffold** for an org choosing to engage the EO Sec. 3
  voluntary framework: a cyber-capability **self-assessment checklist** and the four EO-named
  pre-release control families — **confidentiality, cybersecurity, insider-risk, IP-protection** —
  each slot grounded (cite-or-decline) in NIST AI RMF (S-54). It is a **governance checklist, not
  a model evaluator**; a control with no grounding source is marked `needs_grounding` (mirrors the
  S-8/S-26 decline contract). *Reuses:* the S-26 pillar-review scaffold shape, cite-or-decline.

### Phase S — grounding sources (the fuel for H-14/H-15/H-16/C-22)

- **S-54 — EO security-governance source catalog.** Secures + registers the EO-relevant tier-1
  authorities **not already in the catalog** into a new `standards/eo-security-sources.yaml`
  (NIST SP 800-53/171/RMF are already secured per §27.14 and are reused, not re-added). New seed
  sources (URLs verified at authoring time, `applies_to: security-governance`): `cisa-ssdf`
  (NIST SP 800-218 Secure Software Development Framework), `nist-ai-rmf` (NIST AI 100-1 AI Risk
  Management Framework), `slsa-framework` (SLSA supply-chain levels), `openssf-scorecard`
  (OpenSSF Scorecard checks), `cisa-kev` (CISA Known Exploited Vulnerabilities catalog). *Reuses:*
  the §2.6 two-tier source schema, the S-23/S-30/S-31 "own catalog file, registered namespace"
  pattern (keeps the S-1 "exactly 17 default sources" baseline untouched).

### Phase C — compliance & governance mapping (EO Sec. 2/3/4)

- **C-22 — EO control-mapping + national-security-systems compliance profile.** Registers an EO
  control mapping in the §2.9 schema (`framework: eo-advanced-ai-2026`, control ids `eo-sec2-*`,
  `eo-sec3-*`, `eo-sec4-*`) and adds a `national-security-systems` profile extension layered over
  the existing `government` profile (§2.5). The mapping ties each EO directive to the plugin
  control that satisfies it (Sec. 2 ⇒ H-14/H-15; Sec. 3 ⇒ H-16/H-15; Sec. 4 ⇒ misuse-resistant
  audit trail = C-4 + L-14). Cross-walked to NIST AI RMF, SSDF, and NIST 800-53. *Reuses:* §2.9
  control mapping, §2.5 profile system.
- **C-23 — Coordinated vulnerability-disclosure evidence record.** `commands/cvd-record.sh`
  produces a **clearinghouse-style coordinated-disclosure record** from H-14 findings — the EO
  Sec. 2 "find and fix" loop made auditable: `{advisory_id, severity, component, fixed_in,
  disclosed_at, remediation_status, source}` — and feeds it into the C-4 audit log and the §2.24
  portable audit-pack so it is **exportable for government/agency review**. *Reuses:* C-4 audit
  log, §2.24 audit-pack, L-14 audit-integration pattern.

### Phase X — execution surfaces (three-surface enforcement)

- **X-10 — Vulnerability + SBOM CI gate.** Wires H-14 and H-15 into the closed-loop CI workflow
  (`.github/workflows/closed-loop.yml`) and the `/doctor` surface with the **same exit-code
  contract**, exactly as §27.28/§27.29 wired S-30 into `/doctor` + CI. Safe-by-default: a repo
  with no declared dependencies and no IaC is a green no-op (cannot turn a previously-green repo
  red). *Reuses:* the §27.28/§27.29 `/doctor --check ... --root .` + CI-step pattern, X-1 GitHub
  Actions surface.

---

## §28.2 Cross-cutting contracts

- **§2.30 — Vulnerability-remediation gate contract.** Defines the find→classify→remediate→record
  loop. Severity thresholds: `critical`/`high` ⇒ block (exit 1), `medium`/`low` ⇒ warn (exit 0
  with marker), none ⇒ green no-op (exit 0). **Safe-by-default:** a project with no resolvable
  manifest is `vuln-scan no_dependencies=true status=skipped` (exit 0). Every blocked finding
  names its `fixed_in` remediation (the "and fix" half is mandatory, not advisory). Findings are
  recorded via C-23. Observable markers: `vuln-scan status=green deps=<n> vulnerabilities=0`;
  per-finding `vuln-scan component=<c> severity=<s> fixed_in=<v>`; summary `vuln-scan status=red
  critical=<n> high=<n>`. The advisory dataset is injected (deterministic substrate); the live
  feed is a production edge (mirrors the §27.19 external-call-safety discipline).
- **§2.31 — SBOM + signed-provenance attestation contract.** Every SBOM is emitted in a named
  standard format (`cyclonedx` default | `spdx`) with a `schema_version`, the component inventory,
  and a content digest. The signed provenance attestation binds `{sbom_digest, artifact_digest,
  builder, materials, signature}` and links to the §2.8 AI Provenance Manifest. Signing reuses the
  O-6 / H-4 key-handling trust model; an unsigned run is marked `attestation=unsigned` and never
  claims provenance. SLSA-level claims are grounded in the `slsa-framework` source (cite-or-decline).
- **§2.32 — Frontier pre-release governance contract.** The H-16 readiness checklist MUST cover the
  four EO-named control families — `confidentiality`, `cybersecurity`, `insider_risk`,
  `ip_protection` — for the up-to-30-day pre-release access window. Each control is grounded
  (cite-or-decline) in a secured authority (NIST AI RMF / NIST 800-53); an ungrounded control is
  `needs_grounding`. The checklist is **advisory and voluntary** — it emits no determination, sets
  no threshold, and explicitly records the EO Sec. 1 no-mandatory-licensing disclaimer in its
  header so the artifact cannot be mistaken for a compliance mandate.

---

## §28.3 Spec sketches (10 behaviors per feature, authored at build time)

Representative behaviors (the full 10-per-feature set is written in Step 1 of the build CL, per
the `CLAUDE.md` workflow loop; folder names are the anti-drift map in §28.5):

- **H-14:** blocks on an injected critical CVE and names the fixed-in version · warns on a medium ·
  green no-op when the manifest is clean · `no_dependencies` skip on a manifest-less repo ·
  multi-ecosystem (npm + pip) in one run · respects a configurable threshold override · records
  each finding to C-23 · exit-code contract 0/1 · `--json` shape · grounds severity policy in SSDF.
- **H-15:** emits a CycloneDX SBOM with component inventory · emits SPDX on `--format spdx` ·
  produces a signed attestation binding the SBOM digest · marks `attestation=unsigned` without a
  key · links to the §2.8 manifest · SLSA-level claim cites `slsa-framework` · deterministic digest ·
  `--dry-run` previews · rejects an unknown format (exit 2) · json sidecar.
- **H-16:** scaffolds all four EO control families · grounds each in NIST AI RMF · marks an
  ungrounded control `needs_grounding` · records the Sec. 1 no-licensing disclaimer · emits the
  30-day window · cyber-capability self-assessment checklist present · declines an unknown control ·
  md + json output · voluntary/advisory header · cite-or-decline honored.
- **S-54:** five seed sources registered · each tier-1 with `applies_to: security-governance` ·
  new namespace registered without disturbing the S-1 17-source baseline · URLs well-formed ·
  reused NIST sources not duplicated · `sources-catalog` lists them · schema-valid per §2.6 · etc.
- **C-22:** EO mapping registered under `eo-advanced-ai-2026` · Sec. 2/3/4 each map to a plugin
  control · NSS profile extends `government` · cross-walk to NIST AI RMF/SSDF/800-53 · etc.
- **C-23:** disclosure record carries `fixed_in` · feeds the C-4 audit log · appears in the
  §2.24 audit-pack · `remediation_status` enum · sourced from H-14 findings · etc.
- **X-10:** `/doctor --check vuln-scan --root .` green no-op on a clean repo · CI step appended,
  no existing step altered · same exit-code contract · SBOM step present · safe-by-default · etc.

## §28.4 §20 sequencing (extends the canonical staged path)

EO-driven, deadline-anchored ordering (governance-only ID additions; preserves the §21
definition-of-done — extends surface, not gate):

1. **S-54** first (secure the grounding fuel) — the EO authorities must exist before any feature
   grounds in them.
2. **§2.30 + H-14**, then **C-23** (the Sec. 2 clearinghouse find-and-fix loop + its evidence
   record — the highest-urgency, 30-day-themed deliverable).
3. **§2.31 + H-15** (SBOM + signed provenance — IP protection).
4. **§2.32 + H-16** (Sec. 3 voluntary-framework readiness checklist).
5. **C-22** (the EO control mapping + NSS profile — composes the above into a compliance surface).
6. **X-10** last (wire H-14/H-15 into `/doctor` + CI — three-surface enforcement, mirroring the
   §27.28/§27.29 completion of M6).

Dependencies preserved: S-54 before everything; H-14 before C-23 and X-10; H-15 before X-10;
C-22 composes H-14/H-15/H-16 and so follows them.

## §28.5 Anti-drift exact pending-folder map (per `CLAUDE.md`)

When built, pending specs MUST live at exactly these paths (lowercase phase letter, matching the
constitution's documented convention):

- `evals/pending/h/h-14-dependency-vulnerability-remediation-gate/`
- `evals/pending/h/h-15-sbom-signed-provenance-attestation/`
- `evals/pending/h/h-16-frontier-pre-release-governance-checklist/`
- `evals/pending/s/s-54-eo-security-governance-source-catalog/`
- `evals/pending/c/c-22-eo-control-mapping-and-nss-profile/`
- `evals/pending/c/c-23-coordinated-vulnerability-disclosure-evidence/`
- `evals/pending/x/x-10-vulnerability-and-sbom-ci-gate/`
- `evals/pending/cross-cutting/2-30-vulnerability-remediation-gate-contract/`
- `evals/pending/cross-cutting/2-31-sbom-signed-provenance-attestation-contract/`
- `evals/pending/cross-cutting/2-32-frontier-pre-release-governance-contract/`

New catalog file: `standards/eo-security-sources.yaml` (distinct from the S-1, S-23, S-30/S-31,
S-37 catalogs; their invariants are untouched). New namespace `security-governance` registered in
`generated-code-quality-standards/validate-all.sh` `KNOWN_NAMESPACES` at build time.

## §28.6 §25 fidelity-audit vocabulary additions

The tokens the §25 auditor (`rubric/detectors/audit-pending-spec-fidelity.sh`) must accept in
pending spec bodies for H-14..H-16 / S-54 / C-22 / C-23 / X-10 / §2.30 / §2.31 / §2.32:

`vuln-scan`, `vulnerability`, `remediation`, `remediation_status`, `fixed_in`, `cve`, `cvss`,
`severity`, `critical`, `high`, `medium`, `low`, `advisory`, `advisory_id`, `no_dependencies`,
`dependency`, `sbom`, `cyclonedx`, `spdx`, `attestation`, `provenance`, `signed`, `unsigned`,
`signature`, `sbom_digest`, `artifact_digest`, `builder`, `materials`, `slsa`, `slsa-framework`,
`openssf-scorecard`, `cisa-kev`, `cisa-ssdf`, `ssdf`, `nist-ai-rmf`, `ai-rmf`, `frontier`,
`frontier-eval`, `covered-frontier-model`, `pre-release`, `confidentiality`, `cybersecurity`,
`insider_risk`, `ip_protection`, `needs_grounding`, `voluntary`, `cvd-record`,
`coordinated-disclosure`, `clearinghouse`, `eo-advanced-ai-2026`, `national-security-systems`,
`nss`, `security-governance`, `eo-security-sources`.

## §28.7 Ratification

**Status: PROPOSED.** This reference block is appended without altering any prior §1–§27 content
(`git diff --numstat docs/architecture-v1.9.md` shows 0 deletions). On operator approval ("build
it"), the substrate-and-spec build proceeds per §28.4, one feature (or batch per
`feedback-batch-cl-convention.md`) per CL, each gated by the full workflow loop in `CLAUDE.md`
(Step 0 architecture extraction → Step 1 specs → Step 2 self-audit → Step 3 verify → Step 4 commit).

---

## §28.8 Leadership-panel refinement (additive, 2026-06-13)

A four-persona engineering-leadership review (xAI / Microsoft / Amazon / Apple lenses) of the
PROPOSED v1.18 surface converged on a roadmap that **endorses building H-14/15/16 + provenance**
and recommends a focused set of additions. Filtered to the subset that is concrete,
EO-grounded, architecture-faithful, and **composes existing primitives** (the rest are folded as
refinements or explicitly deferred). Still PROPOSED; no substrate written.

### New feature IDs registered by this refinement

- **W-13 — Adversarial red-team / blue-team review exercise.** `commands/redteam-review.sh`
  orchestrates an adversarial pass over a generated artifact: a blue-team (defensive) and a
  red-team (attacker) subagent argue the artifact's security posture, producing a **red-team
  summary pack** that feeds the H-16 pre-release readiness checklist (EO Sec. 3 contemplates
  pre-release red-teaming/benchmarking) and the C-23 evidence record. Grounded in NIST AI RMF
  red-teaming guidance (S-54 `nist-ai-rmf`) + OWASP. *Reuses:* W-11 parallel subagent
  orchestrator + the existing review subagents + §2.3 findings format; emits into H-16 + C-23.
  Next free ID (Phase W stopped at W-12). Addresses the panel's xAI + Apple "red-team/blue-team
  adversarial testing for model extraction / misuse" recommendation and the deferred Question-2
  option.
- **Q-13 — Security-remediation posture metric.** Extends the §2.11 SPACE metric schema with a
  local-only (Q-6 privacy-by-default, no upload) security-posture dimension: **MTTR-to-remediate**
  (time from H-14 finding to C-23 `remediation_status=fixed`), **vulnerability density**
  (findings per 1k dependencies), and **percent-of-dependencies-with-known-fix-applied**. Makes
  the EO Sec. 2 "find and fix" loop measurable (the panel's Amazon operational-excellence /
  quantifiable-gates recommendation). *Reuses:* §2.11 SPACE schema, H-14 + C-23 outputs. Next
  free ID (Phase Q stopped at Q-12).

### Refinements to already-registered v1.18 features (NO new ID)

- **§2.31 attestation formats broadened.** The signed-provenance attestation accepts, alongside
  the SLSA-style predicate, an **in-toto** attestation predicate, and the signing mechanism may
  be **Sigstore/cosign** (keyless) in addition to the O-6 keyed checkpoint. Format/mechanism is
  named in the artifact (`predicate_type`, `signing_mechanism`); claims still cite-or-decline.
  (Microsoft "CycloneDX + in-toto"; xAI "Sigstore signing default for frontier-adjacent flows".)
- **CISA KEV continuous monitoring = composition, not new engine.** The panel's "automated CISA
  KEV feeds / real-time monitoring" is the existing S-20 in-use poll scheduler + S-21 conditional
  GET applied to the S-54 `cisa-kev` source — no new feature. H-14 reads the polled KEV snapshot
  as one advisory input. Recorded here so it is not later re-invented.
- **C-22/C-23 export breadth.** The compliance mapping export (already §2.24 audit-pack) is noted
  as the carrier for "exportable to Azure Policy / maturity dashboard" — a downstream formatter
  over the existing portable pack, not a new contract. Deferred to a formatter-only follow-up.
- **Critical-infrastructure scaffolds + lighter profiles** are existing surfaces: O-12 application
  scaffolds + the §2.5 profile system (the `national-security-systems` profile C-22 already adds).
  No new ID; a `critical-infrastructure` scaffold/profile is a future O/profile addition.

### Considered and explicitly NOT adopted (flagged, per the drift discipline)

- **"Exploration mode" that bypasses the F-gates / append-only discipline — REJECTED as a
  gate-bypass.** The xAI lens recommends a lightweight mode skipping full gates for spikes. A
  *bypass* directly contradicts the project's core law ("ARCHITECTURE IS LAW", the F-1..F-6 gates,
  append-only). Per the CLAUDE.md drift catalog, flagging is a STOP signal, not a discharge. The
  legitimate intent (velocity for internal spikes) is honored WITHOUT a bypass: spikes run on a
  throwaway branch and **must still reconcile to the manifest before merge** — the gate is deferred
  in time, never skipped. No "skip-gate" flag will be added. If the operator wants a true bypass,
  that is a constitution-level decision requiring its own governance CL, not a v1.18 feature.
- **Differential privacy / on-device evals** (Apple lens) are noted as out-of-scope for v1.18 —
  the actionable residue is **data-minimization rubric rules** (a Phase-E/G rule addition, not an
  EO-security feature). Deferred to a separate rules CL.

### Updated §28.6 vocabulary additions (W-13 / Q-13 / refinements)

`redteam-review`, `red-team`, `blue-team`, `adversarial`, `red-team-pack`, `mttr`,
`mttr_to_remediate`, `vulnerability_density`, `posture`, `security-posture`, `in-toto`,
`predicate_type`, `signing_mechanism`, `sigstore`, `cosign`, `keyless`.

### Anti-drift folder map (W-13 / Q-13)

- `evals/pending/w/w-13-adversarial-red-team-blue-team-review/`
- `evals/pending/q/q-13-security-remediation-posture-metric/`

### §20 sequencing (W-13 / Q-13)

Both ship after their inputs exist: **W-13** after H-16 + C-23 (it feeds them); **Q-13** after
H-14 + C-23 (it measures them). So in the §28.4 order they slot at the end, after X-10.
Governance-only ID additions; preserve the §21 definition-of-done.
