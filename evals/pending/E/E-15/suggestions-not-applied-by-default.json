{
  "name": "suggestions: --fix does NOT apply suggestions (only auto-fixes); file unchanged",
  "command": "cp file.js orig.js && bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule suggest-rename --in \"$PWD/file.js\" --fix && diff file.js orig.js 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'const oldName = 1;' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
