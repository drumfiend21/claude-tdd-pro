{
  "name": "/space-report shows pr-corpus learning rate as a Performance metric in the cross-loop dashboard",
  "command": "mkdir -p .claude-tdd-pro/pr-corpus space && printf '{\"learn_events_30d\":12}\\n' > .claude-tdd-pro/pr-corpus/learn-stats.json && printf 'metrics:\\n  - {id: space-pr-corpus-learn-rate, dimension: performance}\\n' > space/metrics.yaml && printf 'dimensions: {performance: {enabled: true}}\\nshare: never\\n' > space/config.yaml && bash \"$CLAUDE_PLUGIN_ROOT/commands/space-report.sh\" --metrics space/metrics.yaml --config space/config.yaml --since 30d 2>out.txt && grep -q 'space-pr-corpus-learn-rate' out.txt && grep -q '12' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
