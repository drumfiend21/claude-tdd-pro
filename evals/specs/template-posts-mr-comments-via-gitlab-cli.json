{
  "name": "gitlab ci: template uses gitlab-cli (glab or gitlab-runner) to post MR comments with findings",
  "command": "grep -E \"glab|gitlab-cli|gitlab merge_request\" \"$CLAUDE_PLUGIN_ROOT/ci/.gitlab-ci.template.yml\" 1>&2 && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
