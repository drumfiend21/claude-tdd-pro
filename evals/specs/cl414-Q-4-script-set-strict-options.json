{
  "name": "Q-4 friction tracker: script sets strict bash options (set -uo pipefail or similar)",
  "command": "grep -qE \"set -u\" \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/friction-tracker.sh\" && echo strict 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["strict"]}
}
