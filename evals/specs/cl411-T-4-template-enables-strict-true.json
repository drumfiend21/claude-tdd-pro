{
  "name": "T-4 tsconfig.strict.json template: enables strict:true so the five §7 T-4 flags layer on top of the canonical strict family",
  "command": "grep -q '\"strict\": true' \"$CLAUDE_PLUGIN_ROOT/templates/tsconfig.strict.json\" && echo present 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["present"]}
}
