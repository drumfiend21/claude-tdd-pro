{
  "name": "C-8 risk-classify exits two when --use-case or --emit is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/risk-classify.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
