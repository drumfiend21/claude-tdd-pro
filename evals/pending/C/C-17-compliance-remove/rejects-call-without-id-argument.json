{
  "name": "/compliance-remove: invocation with no id argument exits 2 and surfaces a usage error",
  "command": "mkdir -p .claude-tdd-pro && bash \"$CLAUDE_PLUGIN_ROOT/commands/compliance-remove.sh\" --catalog \"$PWD/.claude-tdd-pro/COMPLIANCE-URLS.yaml\"",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "id"
    ]
  }
}
