{
  "name": "polling scheduler: a sub-day cadence fires only when the active-flow stack is non-empty",
  "command": "printf '{\"flow\":\"build-substrate\",\"pid\":1}\\n' > stack.ndjson && bash \"$CLAUDE_PLUGIN_ROOT/standards/poll-scheduler.sh\" --source-id s --fetch-frequency 5m --now-ms 600000 --last-fetch-ms 0 --active-flow-stub \"$PWD/stack.ndjson\"",
  "setup": [],
  "expect": { "exit_code": 0, "stderr_contains": ["decision=fetch", "freshness=fresh-within-fetch-frequency"] }
}
