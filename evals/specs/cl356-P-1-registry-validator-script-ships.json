{
  "name": "P-1 registry validator script ships at prompts/validate-registry.sh per the architecture",
  "command": "[ -s \"$CLAUDE_PLUGIN_ROOT/prompts/validate-registry.sh\" ] && echo ok 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["ok"]}
}
