{
  "name": "enforce reports pass when the external tree is clean",
  "command": "printf 'export const x: number = 1;\\n' > a.ts\nbash $CLAUDE_PLUGIN_ROOT/rubric/enforce.sh --root . --rule g-ts-001",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "enforce rule=g-ts-001 detector=no-any.sh verdict=pass",
      "status=green"
    ]
  }
}
