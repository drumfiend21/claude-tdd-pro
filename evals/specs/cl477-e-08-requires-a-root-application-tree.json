{
  "name": "enforce requires a root application tree to evaluate",
  "command": "bash $CLAUDE_PLUGIN_ROOT/rubric/enforce.sh --rule g-ts-001",
  "setup": [

  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "root"
    ]
  }
}
