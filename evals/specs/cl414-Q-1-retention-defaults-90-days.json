{
  "name": "Q-1 SPACE config: retention_days defaults to 90 per §10 Q-1",
  "command": "grep -qE '^retention_days:[[:space:]]*90' \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" && echo 90_days 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["90_days"]}
}
