{
  "name": "collector tags Performance metrics with source_loop=rubric so cross-loop provenance is visible in space/metrics.yaml",
  "command": "mkdir -p .claude-tdd-pro/rubric-runs space && printf '{\"pass\":7,\"fail\":3}\\n' > .claude-tdd-pro/rubric-runs/last.json && printf 'dimensions: {performance: {enabled: true}}\\nshare: never\\n' > space/config.yaml && bash \"$CLAUDE_PLUGIN_ROOT/space/collector.sh\" --metric rubric-pass-rate --config space/config.yaml --out space/metrics.yaml 2>err.txt && grep -q 'source_loop: rubric' space/metrics.yaml && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
