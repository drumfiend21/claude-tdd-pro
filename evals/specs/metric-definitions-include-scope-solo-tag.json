{
  "name": "every entry in space/metrics.yaml carries scope=solo so the honest-scope marker travels with each metric definition",
  "command": "mkdir -p .claude-tdd-pro/rubric-runs space && printf '{\"pass\":1,\"fail\":1}\\n' > .claude-tdd-pro/rubric-runs/last.json && printf 'dimensions: {performance: {enabled: true}}\\nshare: never\\n' > space/config.yaml && bash \"$CLAUDE_PLUGIN_ROOT/space/collector.sh\" --metric rubric-pass-rate --config space/config.yaml --out space/metrics.yaml 2>err.txt && grep -q 'scope: solo' space/metrics.yaml && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
