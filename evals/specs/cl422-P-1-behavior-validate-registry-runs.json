{
  "name": "P-1 prompt registry: prompts/validate-registry.sh runs against the shipped registry",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/prompts/validate-registry.sh\" 2>err.txt; rc=$?; [ \"$rc\" -le 2 ] && echo validates 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "validates"
    ]
  }
}
