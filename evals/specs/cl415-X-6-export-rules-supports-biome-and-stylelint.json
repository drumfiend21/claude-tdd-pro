{
  "name": "X-6 IDE rules export: supports --format biome|stylelint per the consumer ecosystem",
  "command": "grep -qE \"biome|stylelint\" \"$CLAUDE_PLUGIN_ROOT/commands/export-rules.sh\" && echo biome 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["biome"]}
}
