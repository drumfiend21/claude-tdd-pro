{
  "name": "hook contract: hook scripts exit 2 on non-JSON stdin (treated as block)",
  "command": "echo 'not json' | bash \"$CLAUDE_PLUGIN_ROOT/hooks/echo-input.sh\"",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "json",
      "parse"
    ]
  }
}
