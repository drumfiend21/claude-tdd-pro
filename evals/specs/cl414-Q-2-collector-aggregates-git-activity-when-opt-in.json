{
  "name": "Q-2 collector: aggregates git activity signal per §10 Q-2 (activity opt-in)",
  "command": "grep -qE 'git|activity' \"$CLAUDE_PLUGIN_ROOT/space/collector.sh\" && echo git 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["git"]}
}
