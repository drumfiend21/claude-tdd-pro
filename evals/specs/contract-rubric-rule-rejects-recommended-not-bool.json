{
  "name": "validate-rubric-rule rejects rule whose `recommended` value is a string instead of a boolean",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/validate-rubric-rule.sh\" \"$CLAUDE_PLUGIN_ROOT/evals/fixtures/contract/rubric-rule-invalid-recommended-not-bool.yaml\"",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": ["recommended", "boolean"]
  }
}
