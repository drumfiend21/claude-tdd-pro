{
  "name": "golden oracle: every decision in the live HIPAA booking design is cited and grounded (needs_grounding empty)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/business-translate.sh\" --profile \"$CLAUDE_PLUGIN_ROOT/standards/golden/healthcare-booking-hipaa-profile.json\" --out req.json --now 2026-06-08T00:00:00Z 2>/dev/null; node -e 'const j=JSON.parse(require(\"fs\").readFileSync(\"req.json\",\"utf8\"));const all=Object.values(j.pillars).flat();process.exit(j.needs_grounding.length===0&&all.every(c=>typeof c.source_id===\"string\"&&c.source_id.length>0&&c.grounding===\"grounded\")?0:1)' && echo hipaa-every-decision-cited 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "hipaa-every-decision-cited"
    ]
  }
}
