{
  "name": "pii egress guard: blocks egress payloads containing an EU national ID number (exit 2)",
  "command": "printf 'DNI 12345678Z attached\\n' > payload.txt && bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/pii-egress-guard.sh\" --input \"$PWD/payload.txt\" --check",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "national-id"
    ]
  }
}
