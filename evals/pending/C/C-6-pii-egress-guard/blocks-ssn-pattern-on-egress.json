{
  "name": "pii egress guard: blocks egress payloads containing a US SSN pattern (exit 2)",
  "command": "printf 'My SSN is 123-45-6789, please update records.\\n' > payload.txt && bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/pii-egress-guard.sh\" --input \"$PWD/payload.txt\" --check",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "ssn"
    ]
  }
}
