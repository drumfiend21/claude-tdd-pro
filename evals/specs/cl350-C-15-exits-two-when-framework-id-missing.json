{
  "name": "C-15 freshness gate exits two when --framework-id is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/freshness-gate.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
