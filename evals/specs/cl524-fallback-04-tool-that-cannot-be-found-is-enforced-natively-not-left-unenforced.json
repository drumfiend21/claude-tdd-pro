{
  "name": "a rule whose routed tool cannot be found is enforced natively rather than left not_enforced",
  "command": "printf 'resource \"aws_security_group\" \"b\" {\\n  ingress { cidr_blocks = [\"0.0.0.0/0\"] }\\n}\\n' > bad.tf && RUN_TOOL_FORCE_ABSENT=1 bash \"$CLAUDE_PLUGIN_ROOT/rubric/composite-dispatch.sh\" --file bad.tf --tools trivy,tfsec,terrascan 2>e.txt; grep -q 'fallback=native' e.txt && grep -qE 'status=(green|red)' e.txt && echo enforced-not-unenforced 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "enforced-not-unenforced"
    ]
  }
}
