{
  "name": "Q-2 collector: refuses to collect dimensions where space/config.yaml shows enabled: false (opt-in gate)",
  "command": "grep -qE 'enabled|opt-in|opt_in' \"$CLAUDE_PLUGIN_ROOT/space/collector.sh\" && echo gates 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["gates"]}
}
