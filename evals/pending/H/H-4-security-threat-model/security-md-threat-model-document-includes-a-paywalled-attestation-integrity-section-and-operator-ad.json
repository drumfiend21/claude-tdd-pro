{
  "name": "SECURITY.md threat model document includes a paywalled attestation integrity section and operator-added trust model section",
  "command": "grep -q 'paywalled.attestation integrity' \"$CLAUDE_PLUGIN_ROOT/SECURITY.md\" && grep -q 'operator-added.*trust' \"$CLAUDE_PLUGIN_ROOT/SECURITY.md\" && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
