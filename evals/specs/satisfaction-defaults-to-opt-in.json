{
  "name": "space config satisfaction dimension defaults to opt-in (off until enabled) per architecture defaults",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/space/show-defaults.sh\" --dimension satisfaction 2>out.txt && grep -q 'satisfaction=opt_in' out.txt && grep -q 'enabled=false' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
