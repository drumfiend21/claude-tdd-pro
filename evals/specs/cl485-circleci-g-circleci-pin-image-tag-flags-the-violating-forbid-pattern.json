{
  "name": "circleci enforcement flags a forbid-rule violation: circleci pin image tag",
  "command": "mkdir -p '.circleci' && printf '%s\\n' 'meta: fixture\n:latest\nmore: data' > '.circleci/config.yml' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-circleci-pin-image-tag",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-circleci-pin-image-tag detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
