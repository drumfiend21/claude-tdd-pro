{
  "name": "G-1 directory layout: the node namespace folder ships per the §17 14-default namespaces list",
  "command": "[ -d \"$CLAUDE_PLUGIN_ROOT/generated-code-quality-standards/node\" ] && echo node_ships 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["node_ships"]}
}
