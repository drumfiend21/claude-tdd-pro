{
  "name": "E-12 cache: STATS block emits workers=N counter (parallelism observable)",
  "command": "out=$(bash \"$CLAUDE_PLUGIN_ROOT/evals/runner.sh\" --stats --filter \"cl414-Q-1-space-config-yaml-ships\" 2>&1); echo \"$out\" | grep -qE \"workers=[0-9]+\" && echo workers 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "workers"
    ]
  }
}
