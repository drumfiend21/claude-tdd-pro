{
  "name": "scripts/eval-perf-log.sh reports its usage on --help without running the suite (entry exercised)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/scripts/eval-perf-log.sh\" --help 2>&1 | grep -qi 'usage' && echo eval-perf-usage 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "eval-perf-usage"
    ]
  }
}
