{
  "name": "standards catalog: each entry has the §2.6 operator-facing required fields (id, name, url, tier, applies_to, fetch_frequency)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/validate-standards-catalog.sh\" \"$CLAUDE_PLUGIN_ROOT/standards/sources.yaml\" --check operator-facing && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
