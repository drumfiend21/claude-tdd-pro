{
  "name": "fetch-frequencies registry: a per-registry default overrides the global default",
  "command": "printf 'default: weekly\\nregistries:\\n  standards: 12h\\n' > ff.yaml && bash \"$CLAUDE_PLUGIN_ROOT/standards/fetch-frequency-registry.sh\" --source-id x --registry standards --file \"$PWD/ff.yaml\"",
  "setup": [],
  "expect": { "exit_code": 0, "stderr_contains": ["resolved_frequency=12h", "resolution=per-registry-default"] }
}
