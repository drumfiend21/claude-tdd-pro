{
  "name": "E-12 cache: STATS block emits cache_misses counter (hit + miss observable per §2.17)",
  "command": "out=$(bash \"$CLAUDE_PLUGIN_ROOT/evals/runner.sh\" --stats --filter \"cl414-Q-1-space-config-yaml-ships\" 2>&1); echo \"$out\" | grep -qE \"cache_misses=[0-9]\" && echo miss_counter 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "miss_counter"
    ]
  }
}
