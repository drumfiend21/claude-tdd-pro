{
  "name": "eval spec schema: rejects spec missing required `command` field",
  "command": "echo '{\"name\":\"test\",\"setup\":[],\"expect\":{\"exit_code\":0}}' > spec.json && bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/validate-eval-spec.sh\" spec.json 2>&1 | grep -q \"command\" && echo ok >&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
