{
  "name": "S-16 --apply-to-rules exits two when --tree is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/standards/freshness-gate.sh\" --apply-to-rules --now 2026-05-25T10:00:00Z 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'tree' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
