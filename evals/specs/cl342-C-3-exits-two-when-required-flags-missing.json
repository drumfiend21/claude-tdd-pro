{
  "name": "C-3 emitter exits two when --commit --out or --now are omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/emit-provenance-manifest.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
