{
  "name": "SECURITY.md threat model document includes an MCP token handling section",
  "command": "grep -q '^## MCP token handling' \"$CLAUDE_PLUGIN_ROOT/SECURITY.md\" && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
