{
  "name": "compliance closed-loop validation exits status code zero when every pipeline step passes verification",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/closed-loop-validate.sh\" --synthetic-framework test-fw 2>out.txt; rc=$?; [ \"$rc\" = \"0\" ] && grep -q 'all_steps=passed' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
