{
  "name": "C-17 compliance-remove exits two when --catalog path is omitted or does not exist",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/compliance-remove.sh\" custom-framework 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'catalog' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
