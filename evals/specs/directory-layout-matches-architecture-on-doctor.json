{
  "name": "directory layout: `/doctor` reports the present namespace folders and flags any of the 14 defaults that are missing",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/doctor.sh\" --check directory-layout 2>out.txt && grep -E 'directory-layout: ok' out.txt 1>&2 && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
