{
  "name": "sbom enforcement flags a require-rule violation: sbom include components",
  "command": "printf '%s\\n' 'meta: fixture\nother: value' > 'fixture.cdx.json' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-sbom-include-components",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-sbom-include-components detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
