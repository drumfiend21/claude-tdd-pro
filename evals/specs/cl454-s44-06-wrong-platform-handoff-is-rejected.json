{
  "name": "gcp boundary: a wrong-platform handoff is rejected before any external call",
  "command": "cat > bad.json <<'JEOF'\n{\"platform\":\"aws\",\"selected_option\":{\"option_id\":\"x\"}}\nJEOF\nbash \"$CLAUDE_PLUGIN_ROOT/commands/gcp-boundary.sh\" --handoff bad.json --out r.json",
  "setup": [

  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "validation_failed=wrong-platform"
    ]
  }
}
