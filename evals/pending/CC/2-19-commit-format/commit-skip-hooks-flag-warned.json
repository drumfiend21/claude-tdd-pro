{
  "name": "commit format: detector flags commit messages whose body explicitly mentions --no-verify or --no-gpg-sign",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/validate-commit-msg.sh\" \"$PWD/msg.txt\"",
  "setup": [
    "printf 'fix: x\\n\\nbypassed with --no-verify because of urgency.\\n' > msg.txt"
  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "no-verify"
    ]
  }
}
