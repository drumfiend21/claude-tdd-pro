{
  "name": "C-16 compliance-add exits two when <url> argument is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/compliance-add.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'url' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
