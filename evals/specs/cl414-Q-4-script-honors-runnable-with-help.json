{
  "name": "Q-4 friction tracker: runs with --help to surface its CLI interface",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/friction-tracker.sh\" --help 2>err.txt; rc=$?; [ \"$rc\" -le 2 ] && echo runnable 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["runnable"]}
}
