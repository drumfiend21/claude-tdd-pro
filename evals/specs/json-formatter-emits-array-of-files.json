{
  "name": "reporter: json formatter emits an array of {filePath, messages, errorCount, warningCount}",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule no-eval --severity error --in \"$PWD/file.js\" --format json 2>out && grep -E '\"filePath\":' out 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'eval(x)' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
