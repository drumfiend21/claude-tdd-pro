# Rubric changelog

Per architecture §16 O-10: semver tracking for `rubric/RUBRIC.yaml`. Major
bumps on breaking detector contracts; minor on additive features; patch
on bugfixes. The `.claude-tdd-pro/lock.json` pins the rubric version
that profile snapshots were computed against.

## 1.0.0 — 2026-05-20

- Initial versioned baseline per O-10. Existing 58 active rules, ~38
  source-folder files, full detector contract per §2.2.
