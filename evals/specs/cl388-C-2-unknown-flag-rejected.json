{
  "name": "C-2 control mapping: legal-review-mark rejects unknown flags with exit 2 (strict CLI surface)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/legal-review-mark.sh\" --nonsense-flag 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'unknown flag' err.txt && echo strict 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["strict"]}
}
