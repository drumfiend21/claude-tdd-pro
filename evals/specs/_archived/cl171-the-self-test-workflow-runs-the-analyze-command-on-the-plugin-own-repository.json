{
  "name": "the self-test workflow runs the analyze command on the plugin own repository",
  "command": "grep -qE '/analyze|analyze\\.sh' \"$CLAUDE_PLUGIN_ROOT/.github/workflows/self-test.yml\" && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
