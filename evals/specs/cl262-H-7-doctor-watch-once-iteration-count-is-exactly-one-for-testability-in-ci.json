{
  "name": "/doctor --watch --once iteration count is exactly one for testability in CI",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/doctor.sh\" --watch --once 2>out.txt && grep -q 'iterations=1' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
