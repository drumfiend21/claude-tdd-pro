{
  "name": "metrics: --type values restricted to {counter, gauge, histogram}; others rejected",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/metrics.sh\" --emit foo.bar --type rate --value 1 --file m.jsonl",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "type",
      "counter",
      "gauge",
      "histogram"
    ]
  }
}
