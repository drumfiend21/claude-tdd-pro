{
  "name": "SECURITY.md threat model document includes a signing key handling section",
  "command": "grep -q 'signing key' \"$CLAUDE_PLUGIN_ROOT/SECURITY.md\" && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
