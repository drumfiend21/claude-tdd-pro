{
  "name": "/doctor --watch co-runs with the pr-corpus daily monitor activity in the same iteration",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/doctor.sh\" --watch --once --emit co-runs 2>out.txt && grep -q 'co_run=pr-corpus-monitor' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
