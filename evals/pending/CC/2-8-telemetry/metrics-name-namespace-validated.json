{
  "name": "metrics: metric name must match `^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$` (dotted lowercase)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/metrics.sh\" --emit \"Bad-Name\" --value 1 --file \"$PWD/.claude-tdd-pro/metrics.jsonl\"",
  "setup": [
    "mkdir -p .claude-tdd-pro"
  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "name",
      "format"
    ]
  }
}
