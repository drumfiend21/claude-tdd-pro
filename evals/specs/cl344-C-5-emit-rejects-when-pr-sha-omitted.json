{
  "name": "C-5 --emit exits two when --pr-sha is omitted (cannot generate verifier output without target sha)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/sod-gate.sh\" --emit v.json --verdict concur 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'pr-sha' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
