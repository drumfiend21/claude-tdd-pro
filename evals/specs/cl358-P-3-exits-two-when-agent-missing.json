{
  "name": "P-3 prompt-eval exits two when <agent> argument is omitted",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/prompt-eval.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'agent' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
