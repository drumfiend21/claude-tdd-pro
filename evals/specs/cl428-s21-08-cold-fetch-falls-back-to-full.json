{
  "name": "conditional-get fetcher: a cold fetch with no stored validators falls back to a full fetch",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/standards/conditional-get.sh\" --source-id azure --store-dir \"$PWD/store\" --simulate-status 200 --body-hash h0",
  "setup": [],
  "expect": { "exit_code": 0, "stderr_contains": ["fetch=cold-full", "request_if_none_match=none"] }
}
