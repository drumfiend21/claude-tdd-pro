{
  "name": "ansible enforcement flags a forbid-rule violation: ansible no plaintext password",
  "command": "printf '%s\\n' 'meta: fixture\nansible_password:\nmore: data' > 'fixture.yml' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-ansible-no-plaintext-password",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-ansible-no-plaintext-password detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
