{
  "name": "P-1 each version block declares an eval_pass_rate field per the architecture regression-gating contract",
  "command": "grep -qE '^\\s+eval_pass_rate: [0-9.]+' \"$CLAUDE_PLUGIN_ROOT/prompts/registry.yaml\" && echo ok 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["ok"]}
}
