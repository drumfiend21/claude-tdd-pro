{
  "name": "Q-7 cross-loop integration: emit script runs with --help or no args",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/space/cross-loop-emit.sh\" --help 2>err.txt; rc=$?; [ \"$rc\" -le 2 ] && echo helpable 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["helpable"]}
}
