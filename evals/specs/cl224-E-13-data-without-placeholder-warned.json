{
  "name": "messageId: passing data keys not used by template emits a warning",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule msgid-extra-data --in \"$PWD/file.js\" --strict-data 2>out.txt && grep -E \"unused data key\" out.txt 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'x' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
