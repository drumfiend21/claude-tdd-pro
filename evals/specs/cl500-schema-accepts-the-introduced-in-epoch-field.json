{
  "name": "the rubric-rule schema accepts the introduced_in epoch field",
  "command": "node -e 'const s=require(process.argv[1]);if(!s.properties.introduced_in||s.properties.introduced_in.type!==\"string\")process.exit(1)' \"$CLAUDE_PLUGIN_ROOT/schemas/rubric-rule.schema.json\" && echo schema-has-epoch 1>&2",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "schema-has-epoch"
    ]
  }
}
