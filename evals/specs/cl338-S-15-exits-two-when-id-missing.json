{
  "name": "S-15 standards-remove exits two when --id is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/standards-remove.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
