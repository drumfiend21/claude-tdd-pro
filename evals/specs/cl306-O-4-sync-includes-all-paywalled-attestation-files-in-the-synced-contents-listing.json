{
  "name": "sync includes all paywalled attestation files in the synced contents listing",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/skills/sync/list-contents.sh\" 2>out.txt && grep -q 'attestations' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
