{
  "name": "observability enforcement flags a require-rule violation: observability define alerting",
  "command": "printf '%s\\n' 'meta: fixture\nother: value' > 'fixture.rules.yml' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-observability-define-alerting",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-observability-define-alerting detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
