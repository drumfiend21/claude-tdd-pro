{
  "name": "E-15 ESLint wrap: detector references the eslint executable so the wrap is functional",
  "command": "grep -qE \"eslint|npx eslint\" \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/wrap-eslint.sh\" && echo eslint 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["eslint"]}
}
