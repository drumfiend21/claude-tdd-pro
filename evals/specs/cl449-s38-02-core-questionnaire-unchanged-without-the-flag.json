{
  "name": "data-aware intake: without the flag the core questionnaire is unchanged at nine questions",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/business-intake.sh\" --list-questions 2>/dev/null | node -e 'const q=JSON.parse(require(\"fs\").readFileSync(0,\"utf8\"));process.exit(q.length===9?0:1)' && echo core-unchanged 1>&2",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "core-unchanged"
    ]
  }
}
