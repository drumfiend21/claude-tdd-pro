{
  "name": "lock-acquire registered list includes the three freshness sections per the architecture text",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/lock-acquire.sh\" --list-sections 2>out.txt && [ \"$(grep -cE '^section=.*freshness' out.txt)\" = \"3\" ] && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
