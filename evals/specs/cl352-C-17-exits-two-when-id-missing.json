{
  "name": "C-17 compliance-remove exits two when the positional <id> is omitted",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/compliance-remove.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'id' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
