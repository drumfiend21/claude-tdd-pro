{
  "name": "sbom enforcement flags a require-rule violation: sbom declare spec version",
  "command": "printf '%s\\n' 'meta: fixture\nother: value' > 'fixture.cdx.json' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-sbom-declare-spec-version",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-sbom-declare-spec-version detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
