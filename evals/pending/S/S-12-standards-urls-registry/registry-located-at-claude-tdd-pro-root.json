{
  "name": "STANDARDS-URLS registry: registry file lives at .claude-tdd-pro/STANDARDS-URLS.yaml (operator-editable per S-12)",
  "command": "mkdir -p .claude-tdd-pro && bash \"$CLAUDE_PLUGIN_ROOT/commands/init-guardrails.sh\" --emit-empty-registries && [ -f .claude-tdd-pro/STANDARDS-URLS.yaml ] && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
