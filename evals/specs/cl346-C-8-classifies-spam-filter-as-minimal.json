{
  "name": "C-8 risk-classify classifies spam-filter as minimal per EU AI Act minimal-risk category",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/risk-classify.sh\" --use-case spam-filter --emit r.yaml && grep -q 'classification: minimal' r.yaml && echo minimal 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["minimal"]}
}
