{
  "name": "jenkins enforcement flags a forbid-rule violation: jenkins pin image tag",
  "command": "printf '%s\\n' 'meta: fixture\n:latest\nmore: data' > 'Jenkinsfile' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-jenkins-pin-image-tag",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-jenkins-pin-image-tag detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
