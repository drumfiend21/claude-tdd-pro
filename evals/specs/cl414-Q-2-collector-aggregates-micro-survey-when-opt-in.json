{
  "name": "Q-2 collector: aggregates in-terminal micro-survey signal per §10 Q-2 (satisfaction opt-in)",
  "command": "grep -qE 'micro-survey|micro_survey|satisfaction' \"$CLAUDE_PLUGIN_ROOT/space/collector.sh\" && echo survey 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["survey"]}
}
