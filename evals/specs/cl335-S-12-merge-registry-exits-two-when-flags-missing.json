{
  "name": "S-12 merge-registry exits two when --catalog --operator or --emit are omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/standards/merge-registry.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
