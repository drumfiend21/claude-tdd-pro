{
  "name": "extension fetches only issues tagged with the security label from the configured upstream repos",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/pr-corpus/issue-tracker.sh\" --source cfpb-consumerfinance --label security --upstream-stub fixtures/issues.jsonl --dry-run 2>out.txt && grep -q 'label_filter=security' out.txt && echo ok 1>&2",
  "setup": [
    "mkdir -p fixtures && printf '{\"number\":1,\"labels\":[\"security\"]}\\n' > fixtures/issues.jsonl"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
