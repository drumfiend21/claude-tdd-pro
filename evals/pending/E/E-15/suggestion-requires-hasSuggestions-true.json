{
  "name": "suggestions: rule that emits suggestions but lacks meta.hasSuggestions=true throws on validation",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule suggest-no-flag --in \"$PWD/file.js\"",
  "setup": [
    "echo 'const x=1;' > file.js"
  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "hasSuggestions"
    ]
  }
}
