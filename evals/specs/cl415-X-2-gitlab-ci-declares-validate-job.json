{
  "name": "X-2 GitLab CI adapter: declares the rubric-validate job",
  "command": "grep -qE \"rubric-validate|validate:|validate$\" \"$CLAUDE_PLUGIN_ROOT/.gitlab-ci.yml\" && echo job 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["job"]}
}
