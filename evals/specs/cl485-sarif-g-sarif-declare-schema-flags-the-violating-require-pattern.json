{
  "name": "sarif enforcement flags a require-rule violation: sarif declare schema",
  "command": "printf '%s\\n' 'meta: fixture\nother: value' > 'fixture.sarif' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-sarif-declare-schema",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-sarif-declare-schema detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
