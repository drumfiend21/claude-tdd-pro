{
  "name": "G-1 directory layout: the google namespace folder ships per the §17 14-default namespaces list",
  "command": "[ -d \"$CLAUDE_PLUGIN_ROOT/generated-code-quality-standards/google\" ] && echo google_ships 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["google_ships"]}
}
