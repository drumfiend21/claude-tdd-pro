{
  "name": "bbp enforcement flags a forbid-rule violation: bbp no plaintext secret",
  "command": "printf '%s\\n' 'meta: fixture\npassword:\nmore: data' > 'bitbucket-pipelines.yml' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-bbp-no-plaintext-secret",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-bbp-no-plaintext-secret detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
