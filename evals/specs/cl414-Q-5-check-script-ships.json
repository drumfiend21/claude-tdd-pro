{
  "name": "Q-5 flow guard: skills/flow-guard/check.sh ships so the warning is invocable",
  "command": "[ -s \"$CLAUDE_PLUGIN_ROOT/skills/flow-guard/check.sh\" ] && echo ok 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["ok"]}
}
