- [v1.9 architecture canonical reference](project-v19-architecture-canonical.md) — How to use the architecture text + summary of feature IDs and §2.X contracts. **Always cite by exact ID; never invent numbering.**
- [v1.9 architecture full text](project-v19-architecture-text.md) — Verbatim mirror of [../architecture-v1.9.md](../architecture-v1.9.md), auto-refreshed by `hooks/scripts/sync-memory.sh` on session start. v1.9 base + v1.9.1 §23 (H-12, W-10, X-6, X-7, §2.23, §2.24) + v1.10 §24 (X-8 LSP, X-9 devcontainer, P-10 model router, W-11 parallel subagents, W-12 conversational PR reviewer, O-12 scaffolds). Chapter map preamble at top for quick reference.
- [Self-run gap-check before commit approval](feedback-self-gap-check-before-commit.md) — For every CL: write tests → self-audit (10-per-feature, non-shallow, no opaque IDs, Google best-practices, no architecture deviation) → close gaps → verify → only then ask for commit approval.
- [Implementation workflow checklist (per-CL)](feedback-implementation-workflow-checklist.md) — Reference BEFORE any new substrate Write/Edit. 12 items in 4 tiers: substrate-writing patterns (apostrophes/em-dashes/JSON-compact/macOS-portability/Psych-fallback/Read-before-Edit), process discipline (pre-flight non-negotiable, drift triage Park/Path-rewrite/Path+content, terse vs full commit body, item 12 commit spec patches in same CL), tool-call habits (multi-Edit batching, cached one-liners, background-runner parallelism). Persisted 2026-05-15.
- [Implementation optimization plan (per-CL)](feedback-implementation-optimization-plan.md) — Reference at the START of every implementation-phase CL (alongside the workflow checklist). Specifies 4 small scripts (patch-pending-specs, promote-and-rename, lib/glob.sh, scaffold-detector) plus 1 process change (tiered active suite during iteration) that automate already-repeated mechanical patterns without altering architecture, runner, or audit discipline. Build-order: lazy — when next CL benefits from a script, pause one CL to build it. Persisted 2026-05-15 after 19-CL session validation.
- [Bash 3.2 portability checklist for substrate writing](feedback-bash32-portability-checklist.md) — Read BEFORE any new substrate. Seven recurring portability bugs (no associative arrays, `wc -l` padding, `printf %`, env-var-passing-first, set -u + empty arrays, BSD grep `--`, redirect order) + help-text-to-stderr bonus. Persisted 2026-05-20 after CL-146..196 repeated rediscoveries.
- [Substrate-touch CL batching convention](feedback-batch-cl-convention.md) — Features extending the same substrate file ship as ONE commit with per-feature audit sections in the body (range CL-N..M). Preserves every drift guard, amortizes the full-suite wait. Persisted 2026-05-20 after CL-190..196 H-phase sequence validated the pattern.
- [Architecture backlog — in-use polling frequency](architecture-backlog-in-use-polling.md) — Add `<N>ms` / `<N>s` / `<N>m` in-use polling option to fetch_frequency across all three operator registries; configurable via FETCH-FREQUENCIES.yaml; for next architectural iteration (v1.8 candidate).
- [Architecture backlog — conditional GETs for registry fetchers](architecture-backlog-conditional-gets.md) — Add HTTP If-None-Match / If-Modified-Since to S-2, L-2, compliance fetchers so daily polling is nearly free for stable upstream sources; prerequisite for in-use polling note; v1.8 candidate.
