{
  "name": "perf budget: rule without explicit budget inherits engine.defaultBudget from config",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule no-eval --in \"$PWD/file.js\" --print-effective-budget 2>out.json && grep -E '\"effective_ms_per_kloc\":50' out.json 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'module.exports={engine:{defaultBudget:{ms_per_kloc:50}},rules:{\"no-eval\":\"error\"}}' > rule-engine.config.js",
    "echo 'eval(x)' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
