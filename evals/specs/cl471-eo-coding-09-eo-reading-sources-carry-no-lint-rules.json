{
  "name": "every imported EO reading source carries no lint rules",
  "command": "[ \"$(grep -lE '^rules: \\[\\]' $CLAUDE_PLUGIN_ROOT/generated-code-quality-standards/security-governance/*.yaml | wc -l | tr -d ' ')\" -eq 5 ] && echo all-empty-rules 1>&2",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "all-empty-rules"
    ]
  }
}
