{
  "name": "Q-1 SPACE config: retention defaults to 90-days per arch text §10 Q-1",
  "command": "grep -qE \"^retention:[[:space:]]*90-days\" \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" && echo 90_days 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["90_days"]}
}
