{
  "name": "native enforcement catches an unrestricted-ingress violation even when every routed tool is absent",
  "command": "printf 'resource \"aws_security_group\" \"b\" {\\n  ingress { cidr_blocks = [\"0.0.0.0/0\"] }\\n}\\n' > bad.tf && RUN_TOOL_FORCE_ABSENT=1 bash \"$CLAUDE_PLUGIN_ROOT/rubric/composite-dispatch.sh\" --file bad.tf 2>e.txt; rc=$?; [ \"$rc\" -eq 1 ] && grep -q 'fallback=native' e.txt && grep -q 'status=red' e.txt && echo native-catches-violation 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "native-catches-violation"
    ]
  }
}
