{
  "name": "Q-4 friction tracker: records hook latency via --duration-ms",
  "command": "grep -q -- \"--duration-ms\\|DURATION_MS\" \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/friction-tracker.sh\" && echo latency 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["latency"]}
}
