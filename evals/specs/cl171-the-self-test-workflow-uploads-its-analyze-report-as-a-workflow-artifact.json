{
  "name": "the self-test workflow uploads its analyze report as a workflow artifact",
  "command": "grep -q 'upload-artifact' \"$CLAUDE_PLUGIN_ROOT/.github/workflows/self-test.yml\" && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
