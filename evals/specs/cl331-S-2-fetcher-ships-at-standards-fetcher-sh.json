{
  "name": "S-2 standards fetcher ships at standards/fetcher.sh per the literal architecture path",
  "command": "[ -s \"$CLAUDE_PLUGIN_ROOT/standards/fetcher.sh\" ] && echo ok 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["ok"]}
}
