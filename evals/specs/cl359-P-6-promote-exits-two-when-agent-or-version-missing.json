{
  "name": "P-6 prompt-promote exits two when <agent> or <version> is omitted",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/prompt-promote.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
