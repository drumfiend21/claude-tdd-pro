{
  "name": "compliance: --export stig writes a STIGViewer-compatible XML for compliance auditors",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/compliance-export.sh\" --compliance \"$PWD/COMPLIANCE.yaml\" --format stig --out \"$PWD/out.xml\" && grep -E '<Benchmark.*xmlns' out.xml 1>&2 && echo ok 1>&2",
  "setup": [
    "printf 'PCI-DSS:\\n  6.5.1:\\n    - r1\\n' > COMPLIANCE.yaml"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
