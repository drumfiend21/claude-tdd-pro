{
  "name": "doctor: the cloud-conventions check detects a CloudFormation template by its AWSTemplateFormatVersion marker and enforces it",
  "command": "mkdir -p repo && printf '{\"AWSTemplateFormatVersion\":\"2010-09-09\",\"Resources\":{}}\\n' > repo/stack.json && bash \"$CLAUDE_PLUGIN_ROOT/commands/doctor.sh\" --check cloud-conventions --root repo",
  "setup": [],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "status=red"
    ]
  }
}
