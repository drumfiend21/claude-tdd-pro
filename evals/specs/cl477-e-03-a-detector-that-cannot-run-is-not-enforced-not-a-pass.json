{
  "name": "enforce reports a detector that cannot run as not_enforced rather than a pass",
  "command": "printf 'package main\\n' > a.go\nbash $CLAUDE_PLUGIN_ROOT/rubric/enforce.sh --root . --rule g-ts-001 --paths './**/*.go'",
  "setup": [

  ],
  "expect": {
    "exit_code": 3,
    "stderr_contains": [
      "verdict=not_enforced files_evaluated=1",
      "status=incomplete"
    ]
  }
}
