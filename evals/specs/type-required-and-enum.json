{
  "name": "rule meta: rule.type is required and must be one of {problem, suggestion, layout}",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rubric/detectors/rule-metadata-complete.sh\" \"$PWD/g-bad-001.yaml\"",
  "setup": [
    "printf 'id: g-bad-001\\nname: bad\\ndescription: x\\ndetector: rubric/detectors/x.sh\\nfixable: null\\n' > g-bad-001.yaml"
  ],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "type",
      "required"
    ]
  }
}
