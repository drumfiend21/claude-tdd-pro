{
  "name": "Q-1 SPACE config: share defaults to never per §10 Q-1 privacy posture",
  "command": "grep -qE '^share:[[:space:]]*never' \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" && echo never 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["never"]}
}
