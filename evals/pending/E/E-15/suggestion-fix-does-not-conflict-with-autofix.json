{
  "name": "suggestions: a rule may have both autofix and suggestions; --fix applies only the autofix",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule fix-and-suggest --in \"$PWD/file.js\" --fix && grep -E 'AUTOFIXED' file.js 1>&2 && ! grep -E 'SUGGESTED' file.js 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'TARGET' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
