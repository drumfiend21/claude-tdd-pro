{
  "name": "X-9 cloud devcontainer: .devcontainer/ folder ships at least one config file",
  "command": "n=$(ls -A \"$CLAUDE_PLUGIN_ROOT/.devcontainer\" 2>/dev/null | wc -l | tr -d \" \"); [ \"$n\" -gt 0 ] && echo nonempty 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["nonempty"]}
}
