{
  "name": "helm enforcement flags a forbid-rule violation: helm no latest tag",
  "command": "printf '%s\\n' 'meta: fixture\n:latest\nmore: data' > 'fixture.yaml' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-helm-no-latest-tag",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-helm-no-latest-tag detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
