{
  "name": "C-8 risk-classify classifies chatbot as limited per EU AI Act transparency category",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/risk-classify.sh\" --use-case chatbot --emit r.yaml && grep -q 'classification: limited' r.yaml && echo limited 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["limited"]}
}
