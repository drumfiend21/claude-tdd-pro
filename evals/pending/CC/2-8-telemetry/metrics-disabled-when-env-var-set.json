{
  "name": "metrics: $CLAUDE_TDD_PRO_NO_METRICS=1 disables metrics writes (no-op, exit 0, no file created)",
  "command": "CLAUDE_TDD_PRO_NO_METRICS=1 bash \"$CLAUDE_PLUGIN_ROOT/rubric/metrics.sh\" --emit foo.bar --value 1 --file \"$PWD/m.jsonl\" && [ ! -f m.jsonl ] && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
