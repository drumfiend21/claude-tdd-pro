{
  "name": "data-aware intake: the read-write pattern question is grounded in the Azure data store models guide",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/business-intake.sh\" --with-data --list-questions 2>/dev/null | node -e 'const q=JSON.parse(require(\"fs\").readFileSync(0,\"utf8\"));process.exit(q.find(x=>x.key===\"read_write_pattern\").source_id===\"azure-data-store-models\"?0:1)' && echo rw-grounded 1>&2",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "rw-grounded"
    ]
  }
}
