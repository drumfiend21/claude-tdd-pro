---
name: onboard
description: Survey an unfamiliar codebase, infer conventions, and propose a CLAUDE.md so future sessions have working context. Loads the onboard skill.
disable-model-invocation: true
---

The user is new to this codebase (or wants a structured tour for
future sessions to inherit). Load the `onboard` skill and follow it
precisely.

Optional focus area: $ARGUMENTS
