{
  "name": "messageId: data values inserted as literal strings, not re-templated",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule msgid-injection --in \"$PWD/file.js\" --report-format json 2>out.json && grep -E \"{{evil}}\" out.json 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'x' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
