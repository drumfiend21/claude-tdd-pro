{
  "name": "gitlab ci: template publishes the checkstyle XML as a GitLab Code Quality artifact (artifacts.reports.codequality)",
  "command": "grep -E \"codequality|artifacts:\" \"$CLAUDE_PLUGIN_ROOT/ci/.gitlab-ci.template.yml\" 1>&2 && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
