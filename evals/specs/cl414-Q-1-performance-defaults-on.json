{
  "name": "Q-1 SPACE config: performance defaults to posture: ON per §10 Q-1",
  "command": "grep -A1 performance: \"$CLAUDE_PLUGIN_ROOT/space/config.yaml\" | grep -q \"posture: ON\" && echo on 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["on"]}
}
