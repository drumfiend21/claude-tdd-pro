{
  "name": "space config share field defaults to never (no telemetry endpoint accepted in defaults)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/space/show-defaults.sh\" --field share 2>out.txt && grep -q 'share=never' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
