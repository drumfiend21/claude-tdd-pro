{
  "name": "jenkins enforcement flags a forbid-rule violation: jenkins no plaintext credentials",
  "command": "printf '%s\\n' 'meta: fixture\npassword:\nmore: data' > 'Jenkinsfile' && bash \"$CLAUDE_PLUGIN_ROOT/rubric/enforce.sh\" --root . --rule g-jenkins-no-plaintext-credentials",
  "setup": [

  ],
  "expect": {
    "exit_code": 1,
    "stderr_contains": [
      "rule=g-jenkins-no-plaintext-credentials detector=cloud-guidance-rule.sh verdict=fail",
      "status=red"
    ]
  }
}
