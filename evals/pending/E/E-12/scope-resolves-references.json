{
  "name": "source code analyzer: scope.references resolves variable usages to their definitions",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule scope-resolve --in \"$PWD/file.js\" 2>out.txt && grep -E \"resolved=true\" out.txt 1>&2 && echo ok 1>&2",
  "setup": [
    "printf \"const x = 1; x;\" > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
