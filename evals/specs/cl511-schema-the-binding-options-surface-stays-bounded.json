{
  "name": "the binding object keeps additionalProperties false so options are accepted but invented binding keys are rejected",
  "command": "node -e 'const s=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"));process.exit(s.properties.enforced_by.items.additionalProperties===false?0:1)' \"$CLAUDE_PLUGIN_ROOT/schemas/rubric-rule.schema.json\" && echo binding-surface-bounded 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "binding-surface-bounded"
    ]
  }
}
