#!/usr/bin/env bash
# migrations/0.3.0-to-0.4.0.sh — substrate stub. Real migrations land per-CL.
# Per O-3: migrations preserve user state (operator rules, lock data).
set -uo pipefail
echo "migrate: 0.3.0 -> 0.4.0 (no-op stub; preserves user state)" >&2
exit 0
