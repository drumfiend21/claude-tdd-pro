{
  "name": "pii egress guard: blocks egress payloads containing a US driver's license number (exit 2)",
  "command": "printf 'DL: A1234567 (CA) on file\\n' > payload.txt && bash \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/pii-egress-guard.sh\" --input \"$PWD/payload.txt\" --check",
  "setup": [],
  "expect": {
    "exit_code": 2,
    "stderr_contains": [
      "driver"
    ]
  }
}
