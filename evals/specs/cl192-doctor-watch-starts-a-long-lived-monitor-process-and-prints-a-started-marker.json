{
  "name": "/doctor --watch starts a long-lived monitor process and prints a started marker",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/doctor.sh\" --watch --once 2>out.txt && grep -q 'doctor_watch_started=true' out.txt && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
