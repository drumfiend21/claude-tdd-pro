{
  "name": "with no stated needs the recommender defaults to the best-supported technology (one with its own namespace)",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/recommend-technology.sh\" --umbrella frontend 2>&1 >/dev/null | grep -q 'recommended=react' && echo best-supported-default 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "best-supported-default"
    ]
  }
}
