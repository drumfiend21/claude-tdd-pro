{
  "name": "O-2 global --dry-run: /promote-standard supports --dry-run per the §2.14 destructive command list",
  "command": "grep -q -- '--dry-run' \"$CLAUDE_PLUGIN_ROOT/commands/promote-standard.sh\" && grep -q 'DRY_RUN' \"$CLAUDE_PLUGIN_ROOT/commands/promote-standard.sh\" && echo dry_run_supported 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["dry_run_supported"]}
}
