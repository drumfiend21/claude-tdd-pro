{
  "name": "plugin lifecycle: migrations/<from>-to-<to>.sh exists per-version (e.g. 0.3.0-to-0.4.0.sh) and is invoked by /migrate during upgrade",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/migrate.sh\" --from 0.3.0 --to 0.4.0 --dry-run 2>out.txt && grep -E \"0.3.0-to-0.4.0.sh\" out.txt 1>&2 && echo ok 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
