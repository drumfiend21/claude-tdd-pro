{
  "name": "rubric rule schema validator rejects a rule missing the required id field",
  "command": "printf '{\"name\":\"no-any\"}\\n' > rule.json && bash \"$CLAUDE_PLUGIN_ROOT/rubric/validate-rule-schema.sh\" --rule rule.json 2>err.txt; rc=$?; [ \"$rc\" -ne 0 ] && grep -q 'missing.*id' err.txt && echo blocked 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "blocked"
    ]
  }
}
