{
  "name": "F-2 rubric runner: emits 'Results: N passed, M failed' line in output",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/evals/runner.sh\" --filter \"cl414-Q-1-space-config-yaml-ships\" 2>&1 | grep -qE \"^Results: [0-9]+ passed\" && echo results 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "results"
    ]
  }
}
