{
  "name": "suggestions: each report can include suggest:[{messageId,fix}] returned in message.suggestions",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/rule-engine/cli.sh\" --rule suggest-rename --in \"$PWD/file.js\" --report-format json 2>out.json && grep -E '\"suggestions\":\\[\\{' out.json 1>&2 && echo ok 1>&2",
  "setup": [
    "echo 'const oldName = 1;' > file.js"
  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "ok"
    ]
  }
}
