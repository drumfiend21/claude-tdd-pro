{
  "name": "C-4 --append exits two when --event or --actor is omitted per the entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/audit-log.sh\" --append 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
