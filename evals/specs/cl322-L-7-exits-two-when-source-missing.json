{
  "name": "/pr-corpus-update exits two when --source is not supplied per the L-7 entry contract",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/pr-corpus-update.sh\" 2>err.txt; rc=$?; [ \"$rc\" -eq 2 ] && grep -q 'required' err.txt && echo usage 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": ["usage"]
  }
}
