{
  "name": "plain-language explainer: lists the glossary terms it can explain",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/commands/explain.sh\" --list-terms 2>/dev/null | node -e 'const a=JSON.parse(require(\"fs\").readFileSync(0,\"utf8\"));process.exit(a.includes(\"encryption_at_rest\")&&a.includes(\"multi_az\")&&a.length>=10?0:1)' && echo listed 1>&2",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "listed"
    ]
  }
}
