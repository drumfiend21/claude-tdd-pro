{
  "name": "the dispatch summary records native fallback and a real verdict whenever a routed tool was absent, never a silent skip",
  "command": "printf 'resource \"aws_security_group\" \"b\" {\\n  ingress { cidr_blocks = [\"0.0.0.0/0\"] }\\n}\\n' > bad.tf && RUN_TOOL_FORCE_ABSENT=1 bash \"$CLAUDE_PLUGIN_ROOT/rubric/composite-dispatch.sh\" --file bad.tf 2>e.txt; grep -q 'fallback=native' e.txt && grep -qE 'status=(green|red)' e.txt && echo native-enforces-applicable 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "native-enforces-applicable"
    ]
  }
}
