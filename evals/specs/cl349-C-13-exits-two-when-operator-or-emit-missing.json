{
  "name": "C-13 compliance merge-registry exits non-zero when --operator or --emit is omitted",
  "command": "bash \"$CLAUDE_PLUGIN_ROOT/compliance/merge-registry.sh\" 2>err.txt; rc=$?; [ \"$rc\" -ne 0 ] && echo usage 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["usage"]}
}
