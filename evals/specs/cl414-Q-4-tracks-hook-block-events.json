{
  "name": "Q-4 friction tracker: records hook block events via --event --hook --decision",
  "command": "grep -q -- \"--event\" \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/friction-tracker.sh\" && grep -q -- \"--hook\" \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/friction-tracker.sh\" && echo event_hook 1>&2",
  "setup": [],
  "expect": {"exit_code": 0, "stderr_contains": ["event_hook"]}
}
