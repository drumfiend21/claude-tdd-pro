{
  "name": "the native fallback threads the active config profile through enforcement without crashing",
  "command": "printf 'resource \"x\" \"y\" {}\\n' > ok.tf && printf 'extends: [standard]\\nrules: {}\\n' > ctp.config.yaml && RUN_TOOL_FORCE_ABSENT=1 bash \"$CLAUDE_PLUGIN_ROOT/rubric/composite-dispatch.sh\" --file ok.tf --tools checkov --profile ctp.config.yaml 2>e.txt; rc=$?; [ \"$rc\" -eq 0 ] && grep -q 'fallback=native' e.txt && echo fallback-threads-profile 1>&2",
  "setup": [],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "fallback-threads-profile"
    ]
  }
}
