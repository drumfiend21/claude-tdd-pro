{
  "name": "no-any does not flag an any that appears only inside a line comment",
  "command": "printf '// fail-closed: any policy error routes to a human\\nexport const x = 1;\\n' > a.ts\nbash $CLAUDE_PLUGIN_ROOT/rubric/enforce.sh --root . --rule g-ts-001",
  "setup": [

  ],
  "expect": {
    "exit_code": 0,
    "stderr_contains": [
      "verdict=pass files_evaluated=1"
    ]
  }
}
